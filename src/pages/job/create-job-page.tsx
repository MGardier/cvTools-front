import { Layout } from "@/components/layout";
import { CreateJob } from "@/features/job/components/create-job";

export const CreateJobPage = () => {
  return (
    <Layout>
      <CreateJob />
    </Layout>
  );
};
